info:
  name: Get Patient By Id
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/patients/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
