info:
  name: Update Password
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{baseUrl}}/patients/1/password"
  body:
    type: json
    data: |-
      {
        "password": "nowe-haslo-2026"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
