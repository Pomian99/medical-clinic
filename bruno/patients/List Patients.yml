info:
  name: List Patients
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/patients"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
